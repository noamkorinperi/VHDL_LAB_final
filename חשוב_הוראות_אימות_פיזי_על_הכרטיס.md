# חשוב: הוראות אימות פיזי על הכרטיס — ענף forum-compliance

עודכן: 27.08.2026 IDT

מסמך זה הוא נוהל הקבלה הפיזי עבור הענף `codex/forum-compliance` על כרטיס
DE10-Standard. עד שכל סעיפי החובה שלהלן עוברים ומתועדים, הענף נשאר במצב
**טרם אומת פיזית**. תוצאות שנאספו בעבר מ־`main` אינן תחליף להרצה זו.

## 1. ציוד ותנאי פתיחה

- כרטיס Terasic DE10-Standard וספק הכוח שלו.
- כבל USB-Blaster תקין.
- Quartus Prime Lite 21.1.
- סביבת העבודה על הענף `codex/forum-compliance` וללא שינויי RTL מקומיים לא
  מתועדים.
- מומלץ: טלפון לצילום וידאו; oscilloscope או logic analyzer לבדיקת PWM.

אין לחבר probe לנקודה שאינה מיועדת למדידה. בדיקת LEDR8 בעין מספיקה למעבר
הבסיסי; מדידת oscilloscope היא ראיה משופרת.

## 2. רישום גרסה ובניית SOF חדש

משורש המאגר הרץ:

```text
git switch codex/forum-compliance
git pull --ff-only
git status --short --branch
git rev-parse HEAD
```

רשום בדוח הבדיקה את ה־commit המלא. `git status` צריך להראות שאין שינוי בקבצי
הפרויקט; קבצים מקומיים לא־עקובים שאינם חלק מהבנייה מותרים, אך יש לרשום אותם.

מתוך `Quartus/RV32IMscMCU` הרץ:

```text
compile_stage9.cmd
verify_timing_stage9.cmd
```

תנאי מעבר לפני צריבה:

1. `Quartus Prime Full Compilation was successful` ו־0 errors.
2. `Timing Analyzer was successful` ו־0 errors.
3. התכן fully constrained ל־setup ול־hold, וכל ערכי ה־slack הגרועים חיוביים.
4. נוצר קובץ חדש:

   ```text
   Quartus/RV32IMscMCU/output_files/RV32IMscMCU.sof
   ```

ה־SOF משתמש בקושחת `interrupt/test4` המוגדרת ב־top-level. אין להשתמש ב־SOF
ישן מתיקייה אחרת. ערכי utilization או slack עשויים להשתנות מעט בין build
seeds; קריטריון המעבר הוא שאין שגיאות או הפרות תזמון.

## 3. צריבה לכרטיס

1. כבה את הכרטיס, חבר ספק וכבל USB-Blaster, והדלק אותו.
2. פתח את `Quartus/RV32IMscMCU/RV32IMscMCU.qpf`.
3. פתח `Tools > Programmer`.
4. ב־`Hardware Setup` בחר `DE-SoC [USB-1]` או את מופע USB-Blaster של
   הכרטיס.
5. אם שרשרת JTAG מציגה יותר מרכיב אחד, חבר את ה־SOF לשורת ה־FPGA
   `5CSXFC6D6F31C6` (בדרך כלל רכיב 2). אין לתכנת את ה־HPS או flash.
6. הוסף את `output_files/RV32IMscMCU.sof`, סמן `Program/Configure` ולחץ
   `Start`.
7. ודא שההתקדמות מגיעה ל־100% ללא error ושמור צילום של חלון Programmer.

קבע את `SW9:0` לאפס לפני תחילת רצף הבדיקות. הכפתורים פעילים־נמוך פיזית:
`KEY0` הוא reset; ‏`KEY1`–`KEY3` נקראים כמצב לחוץ פעיל־גבוה בתוך ה־MCU.
פעולת הפסיקה של `KEY1`–`KEY3` מתרחשת **בשחרור** הכפתור. בזמן הלחיצה
`LEDR9` דולק, ובשחרור הוא כבה.

## 4. בדיקת אתחול ו־KEY0/reset

1. מיד לאחר הצריבה ודא שכל ששת צגי HEX מציגים `0` ושהתכן אינו תקוע במצב
   לא ידוע.
2. לחץ ושחרר `KEY1` פעם אחת. רק לאחר השחרור הספירה על HEX צריכה להתחיל.
3. לחץ והחזק `KEY0`: הספירה חייבת להיעצר ויציאות ה־GPIO/HEX לחזור לערכי
   reset.
4. שחרר `KEY0` והמתן לנעילת ה־PLL. התוכנה צריכה לעלות מחדש למצב idle עם
   HEX מאופס; הספירה אינה אמורה להתחדש לפני שחרור חדש של `KEY1`.
5. שחרר `KEY1` שוב ואמת שהספירה חוזרת. כשל באתחול מחדש, יציאות `X`/הבהוב
   קבוע או צורך במחזור מתח הם FAIL.

## 5. בדיקת KEY1 ופסיקות compare

לחץ `KEY0` ושחרר כדי להתחיל ממצב ידוע. בצע ארבע לחיצות `KEY1` נפרדות:

1. בכל לחיצה החזק את `KEY1` כחצי שנייה. `LEDR9` צריך לדלוק בזמן ההחזקה,
   והפעולה החדשה אינה אמורה להתחיל לפני השחרור.
2. שחרר את `KEY1`; ‏`LEDR9` צריך לכבות והמונה המוצג ב־HEX צריך להתקדם.
3. מדוד לפחות ארבעה מרווחים בין עדכוני HEX בכל מצב.
4. ארבעת השחרורים צריכים לעבור במחזור בין תקופות compare של בקירוב
   `1.0 s`, ‏`0.5 s`, ‏`0.25 s` ו־`0.125 s` ולחזור על עצמן. סדר נקודת
   הכניסה למחזור תלוי בערך המצב, ולכן תנאי הקבלה הוא הופעת כל ארבע התקופות
   במחזור יציב — לא תקופה ראשונה מסוימת.
5. ודא שכל שחרור משנה מצב פעם אחת בלבד. קפיצה על פני מצב או מספר אירועים
   עבור שחרור יחיד היא FAIL.

בדיקה זו מאמתת מקצה לקצה את קלט KEY1, ‏IE/IFG, ‏TYPE בזמן INTA, חזרה מ־ISR,
ה־Basic Timer והכתיבה לכל זוגות ה־HEX.

## 6. בדיקת KEY2 ו־PWM

לחץ `KEY0` ושחרר, ואז בצע ארבע לחיצות `KEY2` נפרדות:

1. בכל לחיצה ודא ש־`LEDR9` דולק בזמן ההחזקה ושהמעבר מתבצע בשחרור.
2. לאחר השחרור הראשון הספירה ב־HEX נעצרת ו־PWM מופיע ב־`LEDR8`.
3. שחרורים חוזרים של `KEY2` צריכים לשנות את בהירות `LEDR8`; לאחר reset
   רצף ה־duty המתוכנת הוא בקירוב 50%, ‏25%, ‏12.5% ו־6.25%.
4. ודא שאין קיפאון ושכל שחרור מתקבל פעם אחת.
5. אם קיים oscilloscope/logic analyzer, מדוד על יציאת `LEDR8` תדר של
   כ־5 kHz (מחזור כ־200 us), ותעד לפחות שני ערכי duty שונים.
6. לחץ ושחרר `KEY0`; ‏PWM חייב להיפסק והמערכת לחזור ל־idle.

## 7. בדיקת KEY3, ‏REM ו־DIV

קושחת `test4` מודדת זמן ריצה בזיכרון ואינה מציגה את תוצאות המערכים על LED
או HEX. לכן בדיקת הכרטיס מאמתת completion וחזרה תקינה מהפסיקות; נכונות כל
התוצאות המספריות מכוסה ברגרסיית ModelSim.

1. לחץ `KEY0` ושחרר.
2. לחץ והחזק `KEY3`; ודא ש־`LEDR9` דולק. בשחרור הראשון מתחיל מסלול REM.
3. המתן שנייה וודא שהמערכת נשארת מגיבה.
4. לחץ ושחרר `KEY3` פעם שנייה. השחרור השני מפעיל את מסלול DIV.
5. המתן שנייה, ואז לחץ ושחרר `KEY1`. הספירה על HEX חייבת להתחיל ולהמשיך;
   זה מוכיח ששני מסלולי המחלק הסתיימו, ה־ISR חזר וה־CPU לא נשאר ב־stall.
6. חזור על הרצף לאחר reset לפחות פעם נוספת.

אם `KEY1` אינו מפעיל ספירה אחרי שני שחרורי `KEY3`, או שרק מחזור מתח מחזיר
את המערכת, הבדיקה היא FAIL.

## 8. בדיקת רצף משולב

לאחר reset יחיד בצע ללא צריבה מחדש:

```text
KEY1 release -> המתן לשני עדכוני HEX
KEY2 release -> אמת PWM ב-LEDR8
KEY3 release -> המתן
KEY3 release -> המתן
KEY1 release -> אמת שהספירה חוזרת
KEY0 release -> אמת חזרה ל-idle
```

צלם וידאו רציף של הרצף. מטרתו לגלות בעיות state, ‏IFG ישן או חזרה לא תקינה
מ־DIV/REM שאינן נראות כאשר כל תרחיש נבדק בנפרד.

## 9. מה הבדיקה הפיזית אינה מוכיחה לבדה

הבדיקות לעיל נותנות ראיה פיזית להתנהגות החיצונית ולמסלול הפסיקות המלא, אך
אין להסיק מהן לבדן את כל הפרטים הפנימיים:

- שלושת מופעי ה־PLL הלוגיים ומבנה ה־clock domains נבדקים בדוח הסינתזה;
  ה־Fitter רשאי לשתף משאב פיזי עבור MCLK ו־SMCLK הזהים.
- בעלות `Z` על ה־DATA BUS הפנימי הופכת למרבב בתוך ה־FPGA ונבדקת ב־RTL
  ובבדיקת interconnect, לא באמצעות probe חיצוני.
- כתיבה/קריאה של `BTCAPR`, ארבעת ביטי ה־read-only של `BTCTL2`, ניקוי IFG
  כאשר IE כבוי, וסדר retirement של פסיקה שמגיעה באמצע DIV מכוסים בבדיקות
  ModelSim ייעודיות. כדי לטעון שגם הם נבדקו פיזית נדרשת קושחת diagnostic
  נפרדת שמציגה PASS/FAIL על LED/HEX או capture מתאים ב־SignalTap.

אין לסמן את הבדיקות הפנימיות כ־"אומתו פיזית" רק משום שהדגמת `test4` עברה.

## 10. ראיות ודוח תוצאה

צור תיקייה שאינה מחליפה ראיות קודמות, לדוגמה:

```text
DOC/evidence/forum-compliance-YYYYMMDD-HHMM/
```

שמור בה:

1. `validation.txt` לפי התבנית הבאה.
2. צילום Programmer ב־100%.
3. צילום/וידאו reset ואתחול מחדש.
4. וידאו KEY1 המציג ארבעה קצבים.
5. צילום/וידאו KEY2; ואם קיים, צילום scope עם frequency ו־duty.
6. וידאו רצף KEY3, ‏KEY3, ‏KEY1 והמשך ספירה.
7. צילום או קובץ של תוצאות Quartus ו־TimeQuest.

```text
Board: DE10-Standard
Date/time/timezone:
Tester:
Git commit:
Quartus version:
USB-Blaster identifier:
SOF path and build time:

Build + timing: PASS / FAIL
Program 100%: PASS / FAIL
KEY0/reset: PASS / FAIL
KEY1/release + four compare periods: PASS / FAIL
KEY2/PWM: PASS / FAIL
KEY3 REM/DIV + recovery: PASS / FAIL
Combined sequence: PASS / FAIL
Scope measurement (optional): frequency=____ duty=____
Notes:
Overall: PASS / FAIL
```

## 11. שער קבלה

האימות הפיזי של הענף עובר רק כאשר כל שבעת סעיפי החובה בתבנית הם PASS,
ה־commit תואם לענף המרוחק, וכל הראיות נשמרו. לאחר מכן יש לעדכן את
`README.md`, ‏`DOC/FORUM_COMPLIANCE_STATUS.md` ו־`PROJECT_PLAN.md` עם
תאריך, commit, שם הבודק וקישור לראיות. אם סעיף אחד נכשל, משאירים את אזהרת
"טרם אומת פיזית" ומתעדים את הצעד הראשון שבו ההתנהגות סטתה מהצפוי.
